<Window x:Class="BouncingButtons.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="XML редактор" Height="249" Width="200" Background="#bfe6ff">
    <Window.Resources>
        <Style x:Key="ButtonStyle1" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Нормальное состояние -->
                            <Trigger Property="IsMouseOver" Value="False">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="/button.png" 
                                                Stretch="Fill"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <!-- Наведение мыши -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="/button(pressed).png" 
                                                Stretch="Fill"/>
                                    </Setter.Value>
                                </Setter>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                                       Storyboard.TargetProperty="ScaleX"
                                                       To="1.05" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                                       Storyboard.TargetProperty="ScaleY"
                                                       To="1.05" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                                       Storyboard.TargetProperty="ScaleX"
                                                       To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                                       Storyboard.TargetProperty="ScaleY"
                                                       To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>

                            <!-- Состояние нажатия -->
                            
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5"/>
            <RowDefinition Height="5"/>
            <RowDefinition/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="5"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="5"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
            <ColumnDefinition  Width="5"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>
        <Rectangle Grid.Column="1" Grid.RowSpan="5" Fill="#000"/>
        <Rectangle Grid.Column="3" Grid.RowSpan="5" Fill="#000"/>
        <Rectangle Grid.Row="1" Grid.ColumnSpan="5" Fill="#000"/>
        <Rectangle Grid.Row="3" Grid.ColumnSpan="5" Fill="#000"/>
        <Rectangle Grid.Row="0" Grid.ColumnSpan="5" Fill="#062233"/>
        <Rectangle Grid.Row="4" Grid.ColumnSpan="5" Fill="#062233"/>
        <Rectangle Grid.Column="0" Grid.RowSpan="5" Fill="#062233"/>
        <Rectangle Grid.Column="4" Grid.RowSpan="5" Fill="#062233"/>
        <StackPanel Grid.Row="2" Grid.Column="2" Margin="0">
            <Label Content="XML-редактор"  HorizontalContentAlignment="Center" VerticalContentAlignment="Center" 
                   Height ="40" Width="130" FontWeight="Bold" Foreground="White" 
                   Cursor="Hand" Margin="0,10,0,0">
                <Label.Background>
                    <ImageBrush ImageSource="/label.png" Stretch="Fill"/>
                </Label.Background>
               
            </Label>
            <Button Click="CreateXML" Content="Создать XML" Style ="{StaticResource ButtonStyle1}" Width="100" Height="50" Margin="0,15,0,10"/>
            <Button Click="OpenXML" Content="Открыть файл" Style ="{StaticResource ButtonStyle1}" Width="100" Height="50" />
            
        </StackPanel>
    </Grid>
</Window>
